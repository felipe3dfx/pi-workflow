{
  "name": "@felipe.3dfx/pi-workflow",
  "version": "0.2.0",
  "type": "module",
  "description": "Pi harness package for companion readiness. Product workflow lives in Grupo Ilao engineering skills.",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "workflow",
    "skills",
    "extensions"
  ],
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/felipe3dfx/pi-workflow.git"
  },
  "bugs": {
    "url": "https://github.com/felipe3dfx/pi-workflow/issues"
  },
  "homepage": "https://github.com/felipe3dfx/pi-workflow#readme",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "README.md",
    "scripts/**/*.mjs",
    "tools/pi-sandbox.mjs",
    "package.json",
    "LICENSE",
    "extensions/",
    "assets/"
  ],
  "scripts": {
    "pack:dry-run": "npm pack --dry-run",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check:biome": "biome check --formatter-enabled=false .",
    "check:focused-tests": "node scripts/forbid-focused-tests.mjs",
    "pi:sandbox": "node tools/pi-sandbox.mjs",
    "check:publish": "node scripts/validate-pi-package.mjs",
    "check:release": "node scripts/validate-release.mjs",
    "check:distribution": "node scripts/validate-packed-distribution.mjs",
    "check:typecheck": "tsc --noEmit",
    "check:knip": "knip",
    "check": "npm run check:biome && npm run check:typecheck && npm run check:knip && npm run check:focused-tests && npm run check:publish && npm run check:release && node --test test/*.test.mjs && npm run check:distribution",
    "prepublishOnly": "npm run check"
  },
  "pi": {
    "extensions": [
      "./extensions/pi-workflow.ts"
    ]
  },
  "engines": {
    "node": ">=22.19"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@earendil-works/pi-coding-agent": "0.80.6",
    "@earendil-works/pi-tui": "0.80.6",
    "@types/node": "^22.20.1",
    "knip": "^6.25.0",
    "typebox": "1.1.38",
    "typescript": "^7.0.2"
  }
}
