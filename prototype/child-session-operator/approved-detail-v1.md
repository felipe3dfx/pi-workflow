# Approved prototype: subagent detail v1

Disposition: reference-only. The user approved this page on 2026-09-23. It replaces the `$EDITOR` transcript. It is evidence, not an implementation base.

## Confirmed

- `[↗]` opens this detail inside the TUI.
- The header names the child, its model, and its effort.
- The right side shows the state, the elapsed time, and `[x]`.
- `[x]` closes the viewer and returns to the list. It does not cancel the child.
- `Ctrl+x` cancels the child.
- Thinking can be collapsed.
- `q` or `Esc` also returns to the list.
- In Pi, a click works in fullscreen. Every click also has a key, because regular mode leaves the mouse to the terminal.
