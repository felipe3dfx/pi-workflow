# Approved prototype: todos and coexistence v1

Disposition: reference-only. The user approved the verified grok-build layout on 2026-09-23. It is evidence, not an implementation base.

## Confirmed

- Subagents stay at the top of the transcript.
- Todos sit immediately below that list, before the user message.
- A task is not a subagent row.
- The todo box has bracket corners outside the text column, so both lists share the same horizontal start.
- The box has space above and below, and a `×` that collapses it.
- A pending row is highlighted. A done row uses a green check.
- `h` hides or shows done tasks.
- The line near the input counts running children, not todos.
